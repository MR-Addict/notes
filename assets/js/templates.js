document.addEventListener("DOMContentLoaded", () => {
  lightbox.option({
    fadeDuration: 500,
    resizeDuration: 500,
    albumLabel: "图片 %1/%2",

    disableScrolling: true,
    alwaysShowNavOnTouchDevices: true,
  });
});
