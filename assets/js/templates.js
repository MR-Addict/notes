document.addEventListener("DOMContentLoaded", () => {
  lightbox.option({
    fadeDuration: 500,
    resizeDuration: 200,
    albumLabel: "图片 %1/%2",

    wrapAround: true,
    disableScrolling: true,
    alwaysShowNavOnTouchDevices: true,
  });
});
